{
  "name": "windloo-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -F admin dev",
    "build": "pnpm -F admin build",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": { "typescript": "^5.6.3" }
}
